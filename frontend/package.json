{
  "name": "task-manager-frontend",
  "version": "1.0.0",
  "description": "Task Manager Frontend - TypeScript + Vanilla JS",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:ts && vite build",
    "build:ts": "tsc",
    "build:ts:watch": "tsc --watch",
    "preview": "vite preview",
    "compile": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0",
    "vite": "^5.0.0"
  }
}
